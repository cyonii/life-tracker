import { ADD_RECORD } from '../types';

describe('Types', () => {
  describe('ADD_RECORD', () => {
    it('should return the correct type', () => {
      expect(ADD_RECORD).toEqual('ADD_RECORD');
    });
  });
});
